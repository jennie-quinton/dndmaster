import React from 'react';

import styles from './styles.scss';
import { Heading } from '../basics';

const MenuBar = () => (
  <div className={styles.fullWidth}>
    <Heading level={1}>DnD Master</Heading>
  </div>
);

export default MenuBar;
